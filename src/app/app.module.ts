// ARCHIVO HEREDADO - NO SE UTILIZA
// El proyecto ha sido migrado a standalone components y standalone API
// Bootstrap se realiza en main.ts con bootstrapApplication()
// Ver: src/main.ts, src/app/app.config.ts

import { NgModule } from '@angular/core';

@NgModule({})
export class AppModule { }